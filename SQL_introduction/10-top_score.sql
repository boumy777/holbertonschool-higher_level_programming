-- Sélectionne les colonnes 'score' et 'name' de la table second_table
SELECT score, name
FROM second_table
-- Trie les résultats par score en ordre décroissant (du plus haut au plus bas)
ORDER BY score DESC;
